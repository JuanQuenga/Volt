# Server-only. Never rename these with a VITE_ prefix.
KIOSK_CONVEX_URL=https://your-deployment.convex.cloud
# Must match KIOSK_REQUEST_SECRET in the selected Convex deployment.
KIOSK_REQUEST_SECRET=
