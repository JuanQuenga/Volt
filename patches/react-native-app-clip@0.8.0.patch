diff --git a/plugin/build/withPlist.js b/plugin/build/withPlist.js
index 78fe1526d00d848aa046cd1c50f9c8937dbda451..b1f19687bb81a7ed28c4f275bd2451999faea424 100644
--- a/plugin/build/withPlist.js
+++ b/plugin/build/withPlist.js
@@ -14,24 +14,26 @@ const node_path_1 = __importDefault(require("node:path"));
 const DisallowedPlistKeys = [
     "UIBackgroundModes",
 ];
+const normalizePlist = (value) => JSON.parse(JSON.stringify(value));
 const withPlist = (config, { targetName, deploymentTarget, requestEphemeralUserNotification, requestLocationConfirmation, }) => {
     return (0, config_plugins_1.withInfoPlist)(config, (config) => {
         const targetPath = node_path_1.default.join(config.modRequest.platformProjectRoot, targetName);
+        const isDevelopmentClient = config.developmentClient === true;
         // Info.plist
         const filePath = node_path_1.default.join(targetPath, "Info.plist");
         const infoPlist = {
             NSAppClip: {
-                NSAppClipRequestEphemeralUserNotification: requestEphemeralUserNotification,
-                NSAppClipRequestLocationConfirmation: requestLocationConfirmation,
+                NSAppClipRequestEphemeralUserNotification: requestEphemeralUserNotification === true,
+                NSAppClipRequestLocationConfirmation: requestLocationConfirmation === true,
             },
             NSAppTransportSecurity: {
-                NSAllowsArbitraryLoads: config.developmentClient,
+                NSAllowsArbitraryLoads: isDevelopmentClient,
                 NSExceptionDomains: {
                     localhost: {
-                        NSExceptionAllowsInsecureHTTPLoads: config.developmentClient,
+                        NSExceptionAllowsInsecureHTTPLoads: isDevelopmentClient,
                     },
                 },
-                NSAllowsLocalNetworking: config.developmentClient,
+                NSAllowsLocalNetworking: isDevelopmentClient,
             },
             CFBundleName: "$(PRODUCT_NAME)",
             CFBundleDisplayName: "$(PRODUCT_NAME)",
@@ -55,7 +57,7 @@ const withPlist = (config, { targetName, deploymentTarget, requestEphemeralUserN
         node_fs_1.default.mkdirSync(node_path_1.default.dirname(filePath), {
             recursive: true,
         });
-        node_fs_1.default.writeFileSync(filePath, plist_1.default.build(infoPlist));
+        node_fs_1.default.writeFileSync(filePath, plist_1.default.build(normalizePlist(infoPlist)));
         // Expo.plist
         const expoPlistFilePath = node_path_1.default.join(targetPath, "Supporting/Expo.plist");
         const expoPlist = {
@@ -66,7 +68,7 @@ const withPlist = (config, { targetName, deploymentTarget, requestEphemeralUserN
         node_fs_1.default.mkdirSync(node_path_1.default.dirname(expoPlistFilePath), {
             recursive: true,
         });
-        node_fs_1.default.writeFileSync(expoPlistFilePath, plist_1.default.build(expoPlist));
+        node_fs_1.default.writeFileSync(expoPlistFilePath, plist_1.default.build(normalizePlist(expoPlist)));
         return config;
     });
 };
diff --git a/plugin/build/withPodfile.js b/plugin/build/withPodfile.js
index 1b10c9e67363c64aa2e510a20726d0bf063c2b4a..91e5d8a596148ea24fb3b9313152c71e000dc610 100644
--- a/plugin/build/withPodfile.js
+++ b/plugin/build/withPodfile.js
@@ -13,6 +13,7 @@ const withPodfile = (config, { targetName, excludedPackages }) => {
         (config) => {
             const podFilePath = node_path_1.default.join(config.modRequest.platformProjectRoot, "Podfile");
             let podfileContent = node_fs_1.default.readFileSync(podFilePath).toString();
+            const excludedPackagesJson = JSON.stringify(excludedPackages ?? []);
             const useExpoModules = excludedPackages && excludedPackages.length > 0
                 ? `exclude = ["${excludedPackages.join(`", "`)}"]\n  use_expo_modules!(exclude: exclude)`
                 : "use_expo_modules!";
@@ -48,7 +49,7 @@ target '${targetName}' do
     'node',
     '--no-warnings',
     '--eval',
-    'require(require.resolve(\\'react-native-app-clip\\')+\\'/../../plugin/build/cliPlugin.js\\').run(' + json + ', [])'
+    'require(require.resolve(\\'react-native-app-clip\\')+\\'/../../plugin/build/cliPlugin.js\\').run(' + json + ', ${excludedPackagesJson})'
   ]
 
   config = use_native_modules!(clip_command)
