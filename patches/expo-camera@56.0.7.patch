diff --git a/ios/CameraViewModule.swift b/ios/CameraViewModule.swift
index 03c82a4f55fe386908c7c8af520dc6d4ede89dc7..619cce2fc46e0c80a13037d2adc4804ca0fff06d 100644
--- a/ios/CameraViewModule.swift
+++ b/ios/CameraViewModule.swift
@@ -18,7 +18,9 @@ public final class CameraViewModule: Module, ScannerResultHandler {
     Events("onModernBarcodeScanned")
 
     OnCreate {
-      let permissionsManager = self.appContext?.permissions
+      guard let permissionsManager = self.appContext?.permissions else {
+        return
+      }
       EXPermissionsMethodsDelegate.register(
         [
           CameraOnlyPermissionRequester(),
@@ -206,6 +206,13 @@ public final class CameraViewModule: Module, ScannerResultHandler {
         }
       }
 
+      Prop("focusPoint") { (view, point: [String: CGFloat]?) in
+        guard let point, let x = point["x"], let y = point["y"] else {
+          return
+        }
+        view.focusPoint = CGPoint(x: x, y: y)
+      }
+
       Prop("responsiveOrientationWhenOrientationLocked") { (view, responsiveOrientation: Bool?) in
         if let responsiveOrientation, view.responsiveWhenOrientationLocked != responsiveOrientation {
           view.responsiveWhenOrientationLocked = responsiveOrientation
diff --git a/ios/Current/CameraSessionManager.swift b/ios/Current/CameraSessionManager.swift
index eb8305bb06de98419bef5a6a47892f9dcc3b42ff..84bf6fe6dd66125260cd37a75c1d533fa55d5245 100644
--- a/ios/Current/CameraSessionManager.swift
+++ b/ios/Current/CameraSessionManager.swift
@@ -206,6 +206,32 @@ class CameraSessionManager: NSObject, DeviceDiscoveryDelegate {
     }
   }
 
+  func focus(at point: CGPoint) {
+    guard let device = captureDeviceInput?.device else {
+      return
+    }
+
+    do {
+      try device.lockForConfiguration()
+      defer { device.unlockForConfiguration() }
+
+      if device.isFocusPointOfInterestSupported {
+        device.focusPointOfInterest = point
+      }
+      if device.isFocusModeSupported(.autoFocus) {
+        device.focusMode = .autoFocus
+      }
+      if device.isExposurePointOfInterestSupported {
+        device.exposurePointOfInterest = point
+      }
+      if device.isExposureModeSupported(.continuousAutoExposure) {
+        device.exposureMode = .continuousAutoExposure
+      }
+    } catch {
+      log.info("\(#function): \(error.localizedDescription)")
+    }
+  }
+
   func updateZoom() {
     guard let device = captureDeviceInput?.device, let delegate else {
       return
diff --git a/ios/Current/CameraView.swift b/ios/Current/CameraView.swift
index 17470a95cc0037fa64adc26abb3cc496ab19737e..c646d39f2efbaf8d71cf1ab9411eb20440b81109 100644
--- a/ios/Current/CameraView.swift
+++ b/ios/Current/CameraView.swift
@@ -86,6 +86,15 @@ public class CameraView: ExpoView, EXAppLifecycleListener, EXCameraInterface, Ca
     }
   }
 
+  var focusPoint: CGPoint? {
+    didSet {
+      guard let focusPoint else {
+        return
+      }
+      focusCamera(atLayerPoint: focusPoint)
+    }
+  }
+
   var pictureSize = PictureSize.high {
     didSet {
       updatePictureSize()
@@ -176,6 +185,29 @@ public class CameraView: ExpoView, EXAppLifecycleListener, EXCameraInterface, Ca
     previewLayer = AVCaptureVideoPreviewLayer(session: sessionManager.session)
     previewLayer.videoGravity = .resizeAspectFill
     previewLayer.needsDisplayOnBoundsChange = true
+
+    let tapGesture = UITapGestureRecognizer(target: self, action: #selector(handleFocusTap(_:)))
+    tapGesture.cancelsTouchesInView = false
+    addGestureRecognizer(tapGesture)
+  }
+
+  private func focusCamera(atLayerPoint point: CGPoint) {
+    DispatchQueue.main.async { [weak self] in
+      guard let self else {
+        return
+      }
+      let cameraPoint = self.previewLayer.captureDevicePointConverted(fromLayerPoint: point)
+      self.sessionQueue.async {
+        self.sessionManager.focus(at: cameraPoint)
+      }
+    }
+  }
+
+  @objc private func handleFocusTap(_ gesture: UITapGestureRecognizer) {
+    guard gesture.state == .ended else {
+      return
+    }
+    focusCamera(atLayerPoint: gesture.location(in: self))
   }
 
   public func onAppForegrounded() {
